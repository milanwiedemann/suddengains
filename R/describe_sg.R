#' Show descriptives for the sudden gains datasets
#'
#' @param data A \code{bysg} or \code{byperson} dataset.

describe_sg <- function(data){
  print("This is just a placeholder function at the moment, needs some further thought!")
  }
